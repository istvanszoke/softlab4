package test;

import org.junit.Test;
import static org.junit.Assert.*;

/**
 * This is a proof of concept test class.
 * For some reason it has never failed before.
 * And if out genius is truly great it never will.
 */
public class TravisTester {

    @Test
    public void test() {
        assertEquals(true, true);
    }
}